
Para iniciar el servidor, correr:

node --experimental-specifier-resolution=node index.js


Dentro de cada router, esta comentada cada opcion a usar, ya que no logre hacer un if de los ambientes con import. Si usaba require me fallaba porque estaba usando modulos.

